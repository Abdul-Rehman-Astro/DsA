class Solution:
    def swapBitGame (self,N):
        # code here 
        count = 0
        while( N):
            if(N & 1):
                count+=1
            N= N>>1
        if(count%2==0):
            return 2
        else:
            return 1
